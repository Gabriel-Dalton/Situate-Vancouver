# Environment variables for this project live in the repository root.
#
# Copy the root `.env.example` to `.env` at the repo root (same directory as `Makefile`).
# Django loads `REPO_ROOT/.env` automatically (see `config/settings.py`).
