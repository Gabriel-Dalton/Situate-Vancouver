---
description: English for comments, docs, and plans; project documentation in docs/
alwaysApply: true
---

# Comments, documentation, and plans

## Code comments (English only)

- Write **all** inline and block comments in source files in **English** (including `//`, `/* */`, `#`, docstrings, JSDoc/TSDoc, etc.).
- Do not use other languages for comments, including Persian/Farsi.
- **User-facing strings** (UI copy, API messages shown to end users) may stay in the product’s target language if the project requires it—this rule targets **developer comments** only.

```typescript
// ❌ BAD (non-English developer comment)
// Calculer le total

// ✅ GOOD
// Compute the running total
```

## Documentation location

- Store **project documentation** under **`docs/`** at the **repository root** (same level as `README.md` if present).
- Examples: architecture notes, how-to guides, ADRs, API docs written as Markdown, design specs meant for the team.
- Prefer **`docs/<topic>.md`** (or a small subdirectory tree under `docs/` for larger sets) instead of leaving loose `NOTES.md` or `GUIDE.md` files in random folders.
- The root **`README.md`** may remain at the root as the primary entry point; link from it into `docs/` when detail lives there.

## Documentation and plans (English only)

- Write **all documentation** you create or edit in **English**: Markdown under `docs/`, `README` sections, ADRs, design notes, API descriptions, and any other repo docs.
- Write **all plans** in **English**: implementation plans, task breakdowns, phased roadmaps, and structured step-by-step outlines—whether saved as files (e.g. `PLAN.md`, specs) or given as numbered plans in the assistant response.
- Do not use other languages for this material (including Persian/Farsi), so it stays consistent for the team and version control.
- **Exception**: **User-facing product copy** may follow the product’s locale if the project requires it; this rule applies to **developer-facing** docs and plans.
